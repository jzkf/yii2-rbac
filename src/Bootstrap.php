<?php

namespace jzkf\rbac;

use Yii;

/**
 * Bootstrap class for RBAC module
 * 用于模块的自动引导
 */
class Bootstrap implements \yii\base\BootstrapInterface
{
    /**
     * {@inheritdoc}
     */
    public function bootstrap($app)
    {
        // 可以在这里添加模块启动时的初始化代码
        // 例如：注册事件处理器、配置组件等
    }
}
